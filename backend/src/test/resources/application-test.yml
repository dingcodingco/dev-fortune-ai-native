spring:
  datasource:
    url: jdbc:h2:mem:devfortune_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

# 컨텍스트 로드용 더미 값 (실제 외부 호출 없음)
anthropic:
  api:
    key: test-key
    model: claude-3-5-haiku-20241022
